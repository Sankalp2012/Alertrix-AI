# Alertrix-AI
Python application to fetch data related to alert and decide if we need to trigger alert.

Always activate venv before installing packages for this project.
To create same environment, run pip install -r requirements.txt
