services:
  - type: web
    name: alertrix-ai
    runtime: python
    buildCommand: python -m pip install -r requirements.txt
    startCommand: uvicorn app.api:app --host 0.0.0.0 --port 8000
    envVars:
      - key: GROQ_API_KEY
        sync: false
